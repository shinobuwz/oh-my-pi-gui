# Project Knowledge

Pi 会话浏览器 GUI（E:/gitlab/oh-my-pi-gui）。Surface 与召回入口：

- Domain：`.aiknowledge/domain.md`（术语，无 index，按 heading 读）。
- Pitfall：`.aiknowledge/pitfalls/index.md`（先读 index，命中再读 entry）。
- Codemap：`.aiknowledge/codemap/index.md`（先读 index，entry 只给入口/边界，仍需回源）。

知识为 advisory context，不替代源码、测试与 runtime 验证。
